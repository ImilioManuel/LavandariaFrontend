import { Routes } from '@angular/router';
import { HomepageComponent } from './module/site/page/homepage/homepage.component';

export const routes: Routes = [
    {
        path: '', 
        component: HomepageComponent,
    }
];
